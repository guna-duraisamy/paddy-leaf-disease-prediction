# Training images go here

Create the four class folders described in `../README.md`, then place rice-leaf photos in each folder. This directory is ignored by Git to prevent large or unlicensed datasets from being uploaded accidentally.
